<div class="w-10/12 mx-auto p-3 m-16">

<div>
<p> carrousel patrimoines</p>
</div>

<!--
<div class=“flex overflow-auto”>
  <% @actualities_rest.each do |actuality| %>
         <%= link_to "/actualités/#{actuality.id}" do %>

          <div class="card-product d-block w-80" alt="...">
            <img src="<%= actuality.image %>" />
            <div class="card-product-infos">
              <h2><%= actuality.titre %></h2>
              <p><%= actuality.description %></p>
            </div>
          </div>

        <% end %>
      <% end %>
  </div>
-->

<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <% if @actualities.length > 0  %>
        <%= link_to "/actualités/#{@actualities.first.id}" do %>
          <div class="card-product" alt="...">
            <img src="<%= @actualities.first.image %>" />
            <div class="card-product-infos">
                <h2><%= @actualities.first.titre %></h2>
                <p><%= @actualities.first.description %></p>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
      <% @actualities_rest.each do |actuality| %>
         <%= link_to "/actualités/#{actuality.id}" do %>
    <div class="carousel-item">
          <div class="card-product" alt="...">
            <img src="<%= actuality.image %>" />
            <div class="card-product-infos">
              <h2><%= actuality.titre %></h2>
              <p><%= actuality.description %></p>
            </div>
          </div>
    </div>
        <% end %>
      <% end %>
  </div>
<!--
    <div class="carousel-item">
      <img src="https://picsum.photos/1000/300" class="d-block w-100" alt="...">
    </div>
-->

  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>

<div>
<p> carroussel activités</p>
</div>

<div>
<p> </p>
</div>

